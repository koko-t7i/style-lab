---
description: Generate distinct visual design directions as runnable HTML mockups
argument-hint: <product brief, URL, reference style, or iteration request>
---

# Style Lab

Use the `style-lab` skill for this request. If the Skill tool is available, invoke `style-lab` before continuing, then follow that skill's workflow exactly.

User request:

```text
$ARGUMENTS
```

If the user did not provide enough context, ask only the minimum questions required by the `style-lab` workflow.
